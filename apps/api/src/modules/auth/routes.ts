import type { FastifyPluginAsync } from "fastify";

export const registerAuthRoutes: FastifyPluginAsync = async () => {
  // Placeholder module boundary. Auth is intentionally not implemented in this phase.
};
