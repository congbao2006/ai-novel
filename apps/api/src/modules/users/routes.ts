import type { FastifyPluginAsync } from "fastify";

export const registerUsersRoutes: FastifyPluginAsync = async () => {
  // Placeholder module boundary. User APIs are intentionally not implemented yet.
};
