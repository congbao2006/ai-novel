import type { FastifyPluginAsync } from "fastify";

export const registerAiRoutes: FastifyPluginAsync = async () => {
  // Placeholder module boundary. Live AI integration is intentionally not implemented yet.
};
