import type { FastifyPluginAsync } from "fastify";

export const registerStoriesRoutes: FastifyPluginAsync = async () => {
  // Placeholder module boundary. Story catalog APIs are intentionally not implemented yet.
};
